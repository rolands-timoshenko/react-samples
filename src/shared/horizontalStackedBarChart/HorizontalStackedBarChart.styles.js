const HorizontalStackedBarChartStyles = theme => ({
  root: {
    display: "flex",
    height: "100%",
    minWidth: 50,
    paddingTop: "0.1em"
  }
});

export default HorizontalStackedBarChartStyles;
