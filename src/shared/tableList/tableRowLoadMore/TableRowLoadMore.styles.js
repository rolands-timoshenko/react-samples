const TableRowProcessingStyles = theme => ({
  loader: {
    minWidth: 500,
    display: "flex",
    alignItems: "center",
    color: theme.palette.grey["600"]
  },
  row: {
    height: "auto"
  }
});

export default TableRowProcessingStyles;
