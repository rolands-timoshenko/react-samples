import React from "react";
import ReactMarkdown from "react-markdown";

// For propTypes check https://www.npmjs.com/package/react-markdown
const TurbotMarkdown = props => {
  return <ReactMarkdown {...props} />;
};

export default TurbotMarkdown;
