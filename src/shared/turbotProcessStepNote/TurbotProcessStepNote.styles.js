const TurbotProcessStepNoteStyles = theme => ({
  root: {
    display: "flex",
    border: `1px solid ${theme.palette.grey["300"]}`,
    borderTop: "none",
    marginTop: 0,
    width: "100%",
    minHeight: 20,
    padding: "10px 10px"
  }
});

export default TurbotProcessStepNoteStyles;
