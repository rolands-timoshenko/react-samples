import React from "react";

const TurbotDiffAdd = ({ children }) => {
  const style = {
    backgroundColor: "#e6ffed"
  };

  return <div style={style}>{children}</div>;
};

export default TurbotDiffAdd;
