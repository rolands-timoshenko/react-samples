import React from "react";

const TurbotDiffDel = ({ children }) => {
  const style = {
    backgroundColor: "#ffeef0"
  };

  return <div style={style}>{children}</div>;
};

export default TurbotDiffDel;
