const PageContentHeadlineStyles = theme => ({
  root: {
    borderBottom: `1px solid ${theme.palette.appBar.divider}`,
    marginBottom: "1.1rem",
    paddingBottom: "0.7rem"
  }
});

export default PageContentHeadlineStyles;
