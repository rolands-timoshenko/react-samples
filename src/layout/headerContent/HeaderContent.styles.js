const HeaderContentStyles = theme => {
  return {
    root: {
      backgroundColor: theme.palette.background.general,
      paddingTop: theme.spacing.unit * 2.2,
      paddingRight: 0,
      paddingBottom: 0,
      paddingLeft: 0,
      width: "100%"
    }
  };
};

export default HeaderContentStyles;
