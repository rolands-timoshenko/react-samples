const ContentSectionStyles = theme => ({
  horizontalRule: {
    color: theme.palette.appBar.divider,
    margin: "0.5rem 0"
  },
  content: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between"
  }
});

export default ContentSectionStyles;
