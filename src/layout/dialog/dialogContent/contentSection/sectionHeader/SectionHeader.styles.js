const SectionHeaderStyles = theme => ({
  root: {
    marginTop: 0,
    letterSpacing: "1px"
  },
  alignRight: {
    color: theme.palette.secondary.main,
    cursor: "pointer"
  }
});

export default SectionHeaderStyles;
