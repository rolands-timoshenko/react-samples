const ResourceBrowserStyles = theme => ({
  browse: {
    backgroundColor: "transparent",
    boxShadow: "none",
    width: "auto",
    fontSize: theme.typography.body2.fontSize,
    marginLeft: 0,
    marginRight: 0,
    marginBottom: 10
  }
});

export default ResourceBrowserStyles;
