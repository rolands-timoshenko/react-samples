const BrowseResourceMenuStyles = theme => {
  return {
    root: {
      padding: 0,
      fontSize: theme.typography.subtitle1.fontSize
    },
    menuItem: {
      fontSize: theme.typography.body1.fontSize
    }
  };
};

export default BrowseResourceMenuStyles;
