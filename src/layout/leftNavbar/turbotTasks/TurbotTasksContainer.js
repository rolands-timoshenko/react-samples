import React from "react";
import TurbotTasks from "./TurbotTasks";

const handleTasksClick = () => {
  //TODO
};

const TurbotTasksContainer = () => {
  return <TurbotTasks taskCount={0} />;
};

export default TurbotTasksContainer;
